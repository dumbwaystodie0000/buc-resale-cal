# Allow build scripts for required dependencies
public-hoist-pattern[]=*
enable-pre-post-scripts=true
unsafe-perm=true

# Allow specific build scripts that are being blocked
enable-scripts=true
script-shell=bash

# Allow @tailwindcss/oxide and sharp build scripts
auto-install-peers=true
shamefully-hoist=true 